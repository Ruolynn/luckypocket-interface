[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.20"
optimizer = true
optimizer_runs = 200
via_ir = true

[etherscan]
sepolia = { key = "${ETHERSCAN_API_KEY}" }
# 支持 Base Sepolia (如果需要)
# base_sepolia = { key = "${ETHERSCAN_API_KEY}" }

# 通用 Etherscan API Key（用于所有支持的网络）
etherscan = { key = "${ETHERSCAN_API_KEY}" }


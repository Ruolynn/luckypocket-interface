# Cursor 多窗口并行开发指南

## ✅ 简短回答

**是的，可以！** Cursor 支持同时打开多个窗口编辑同一个项目，互不干扰。

---

## 🚀 快速开始

### 方法 1：从现有窗口打开新窗口

在当前 Cursor 窗口中：
- **macOS**: `Cmd + Shift + N`
- **Windows/Linux**: `Ctrl + Shift + N`

然后打开同一个项目文件夹。

### 方法 2：直接从 Finder/资源管理器

1. 在 Finder 中找到项目文件夹
2. 右键点击
3. 选择 "Open with Cursor"
4. 重复以上步骤打开第二个、第三个窗口

### 方法 3：使用命令行

```bash
# 打开第一个窗口
cursor /Users/lushengqi/工作间/Github/HongBao

# 新开终端或终端标签，打开第二个窗口
cursor -n /Users/lushengqi/工作间/Github/HongBao

# -n 参数表示新窗口 (new window)
```

---

## 📋 需要做的准备

### 1. Git 分支策略（强烈推荐）

为避免冲突，建议每个窗口工作在不同的分支上：

```bash
# 窗口 1 - 开发 Linear 集成
git checkout -b feature/linear-integration

# 窗口 2 - 修复红包 Bug  
git checkout -b fix/packet-claim-issue

# 窗口 3 - 开发前端功能
git checkout -b feature/ui-updates
```

**好处**：
- ✅ 避免文件冲突
- ✅ 独立的工作上下文
- ✅ 可以随时切换焦点
- ✅ 更清晰的 Git 历史

### 2. 配置文件管理

#### 选项 A：共享配置（推荐）

所有窗口共享相同的 `.env` 和配置文件：

```
HongBao/
├── apps/
│   └── api/
│       └── .env          # 共享配置
```

**优点**：配置统一，不需要额外管理  
**缺点**：不能同时测试不同配置

#### 选项 B：独立配置（高级用途）

为不同窗口准备不同的环境配置：

```bash
# 窗口 1 - 使用开发环境
cp .env .env.dev
# 修改端口为 3001

# 窗口 2 - 使用测试环境
cp .env .env.test  
# 修改端口为 3002

# 窗口 3 - 使用 Staging 环境
cp .env .env.staging
# 修改端口为 3003
```

然后在启动时指定：
```bash
# 窗口 1
PORT=3001 pnpm dev

# 窗口 2
PORT=3002 pnpm dev

# 窗口 3
PORT=3003 pnpm dev
```

### 3. 数据库隔离（如需要）

如果需要独立的数据库环境：

```bash
# 窗口 1 - 主开发数据库
DATABASE_URL=postgresql://postgres:password@localhost:5432/hongbao_dev

# 窗口 2 - 测试数据库
DATABASE_URL=postgresql://postgres:password@localhost:5432/hongbao_test

# 窗口 3 - 功能测试数据库
DATABASE_URL=postgresql://postgres:password@localhost:5432/hongbao_feature
```

### 4. 端口管理

确保不同服务使用不同端口：

```bash
# 窗口 1 - API 服务
PORT=3001

# 窗口 2 - Web 前端
PORT=3000

# 窗口 3 - 另一个 API 实例
PORT=3002
```

---

## 🎯 典型使用场景

### 场景 1：前后端并行开发

```
窗口 1: /HongBao/apps/api      (后端开发)
窗口 2: /HongBao/apps/web      (前端开发)
窗口 3: /HongBao/packages/contracts  (合约开发)
```

**配置**：
```bash
# 窗口 1
cd apps/api
pnpm dev  # 端口 3001

# 窗口 2
cd apps/web
pnpm dev  # 端口 3000

# 窗口 3
cd packages/contracts
forge test --watch
```

### 场景 2：功能开发 + Bug 修复

```
窗口 1: feature/new-feature 分支 (新功能)
窗口 2: fix/urgent-bug 分支     (紧急修复)
窗口 3: main 分支                (代码审查)
```

### 场景 3：多模块并行

```
窗口 1: Linear 集成开发
窗口 2: 红包核心逻辑
窗口 3: 成就系统开发
```

---

## ⚠️ 注意事项

### 1. 文件修改冲突

**问题**：两个窗口同时编辑同一个文件

**解决方案**：
- ✅ 使用不同分支
- ✅ 编辑不同的文件
- ✅ Cursor 会实时显示文件被外部修改的提示
- ✅ 及时保存和提交更改

**Cursor 的处理**：
- 会提示 "文件已被外部修改"
- 可以选择重新加载或保留当前更改
- 不会丢失未保存的更改

### 2. Git 操作

**建议**：
- 每个窗口独立的分支
- 频繁提交小更改
- 使用 `git status` 确认当前分支

**命令**：
```bash
# 查看当前分支
git branch --show-current

# 查看状态
git status

# 提交前检查
git diff
```

### 3. 终端进程

**问题**：多个窗口可能启动多个服务进程

**解决方案**：
```bash
# 检查端口占用
lsof -i :3001

# 杀死进程
kill -9 <PID>

# 或者使用不同端口
PORT=3002 pnpm dev
```

### 4. 内存使用

多个 Cursor 窗口会占用更多内存：
- 每个窗口: ~200-500MB
- 建议内存: 16GB+
- 必要时关闭不用的窗口

### 5. AI 上下文

**重要**：每个 Cursor 窗口有独立的 AI 对话上下文
- 窗口 1 的对话历史不会传到窗口 2
- 需要在每个窗口重新描述背景
- 可以使用 @files 引用相同的文件

---

## 🛠️ 推荐配置

### 1. Workspace 设置

在项目根目录创建 `.vscode/settings.json`（Cursor 兼容）：

```json
{
  "files.watcherExclude": {
    "**/node_modules/**": true,
    "**/.git/objects/**": true,
    "**/.git/subtree-cache/**": true,
    "**/dist/**": true
  },
  "search.exclude": {
    "**/node_modules": true,
    "**/dist": true,
    "**/.git": true
  },
  "files.autoSave": "afterDelay",
  "files.autoSaveDelay": 1000
}
```

### 2. Git 配置

```bash
# 设置自动重新加载
git config --global core.autorefresh true

# 设置合并工具
git config --global merge.tool cursor
```

### 3. 建议的文件夹结构

```
HongBao/
├── apps/
│   ├── api/          # 窗口 1: 后端开发
│   └── web/          # 窗口 2: 前端开发
├── packages/
│   └── contracts/    # 窗口 3: 合约开发
└── docs/            # 任意窗口: 文档编写
```

---

## 💡 最佳实践

### 1. 分支命名规范

```bash
# 功能开发
git checkout -b feature/linear-integration
git checkout -b feature/achievement-system

# Bug 修复
git checkout -b fix/packet-claim-error
git checkout -b fix/memory-leak

# 重构
git checkout -b refactor/api-structure
```

### 2. 工作流程

```bash
# 窗口 1: 开发新功能
cd /path/to/project
git checkout -b feature/new-feature
cursor .

# 窗口 2: 修复 Bug
cd /path/to/project
git checkout -b fix/bug-123
cursor -n .

# 窗口 3: 代码审查
cd /path/to/project
git checkout develop
cursor -n .
```

### 3. 任务分配示例

**窗口 1 - API 后端**：
```typescript
// 专注于 Linear 服务开发
apps/api/src/services/linear.service.ts
apps/api/src/routes/linear.ts
```

**窗口 2 - Web 前端**：
```typescript
// 专注于 Linear UI 集成
apps/web/src/components/LinearIssueList.tsx
apps/web/src/hooks/useLinear.ts
```

**窗口 3 - 文档和配置**：
```markdown
docs/Linear集成指南.md
README.md
```

---

## 🚦 快速检查清单

在开启多窗口开发前，确认：

- [ ] **Git 分支策略**：每个窗口使用不同分支
- [ ] **端口分配**：避免端口冲突（3001, 3002, 3003...）
- [ ] **数据库隔离**：如需要，使用不同数据库
- [ ] **环境变量**：确认配置正确
- [ ] **内存充足**：至少 16GB RAM
- [ ] **文件自动保存**：启用 auto-save
- [ ] **Git 状态**：定期检查 `git status`

---

## 🎨 推荐工作布局

### 布局 1：双显示器

```
┌─────────────────┐  ┌─────────────────┐
│   显示器 1       │  │   显示器 2       │
│                 │  │                 │
│  Cursor 窗口 1  │  │  Cursor 窗口 2  │
│  (API 后端)     │  │  (Web 前端)     │
│                 │  │                 │
│                 │  │  浏览器测试      │
└─────────────────┘  └─────────────────┘
```

### 布局 2：单显示器

使用 macOS Mission Control 或 Windows 虚拟桌面：

```
桌面 1: Cursor 窗口 1 (API)
桌面 2: Cursor 窗口 2 (Web)  
桌面 3: 浏览器 + 终端
```

快捷键切换：
- macOS: `Ctrl + ←/→`
- Windows: `Ctrl + Win + ←/→`

### 布局 3：分屏

```
┌───────────────────────────┐
│   Cursor 1    │ Cursor 2  │
│   (主要开发)  │ (参考代码)│
│               │           │
├───────────────────────────┤
│   终端                    │
└───────────────────────────┘
```

---

## 🔧 高级技巧

### 1. 使用 tmux/screen 管理多个终端会话

```bash
# 创建会话
tmux new -s api
tmux new -s web
tmux new -s contracts

# 切换会话
tmux attach -t api
```

### 2. 使用脚本快速启动多窗口环境

创建 `scripts/dev-multi-window.sh`：

```bash
#!/bin/bash

# 窗口 1 - API
cursor /Users/lushengqi/工作间/Github/HongBao &
sleep 2

# 窗口 2 - Web
cursor -n /Users/lushengqi/工作间/Github/HongBao &
sleep 2

# 窗口 3 - Contracts
cursor -n /Users/lushengqi/工作间/Github/HongBao &

echo "已启动 3 个 Cursor 窗口"
```

### 3. 使用 Git Worktree（高级）

如果需要完全独立的工作目录：

```bash
# 创建 worktree
git worktree add ../HongBao-feature-1 feature/linear
git worktree add ../HongBao-feature-2 feature/achievement
git worktree add ../HongBao-bugfix fix/urgent

# 每个目录独立打开
cursor ../HongBao-feature-1
cursor ../HongBao-feature-2
cursor ../HongBao-bugfix
```

---

## 📊 性能优化

### 如果 Cursor 变慢

1. **关闭不必要的窗口**
2. **减少文件监控范围**
3. **禁用不需要的插件**
4. **清理 node_modules**：
```bash
# 只在需要时安装依赖
pnpm install --prefer-offline
```

5. **增加 Cursor 内存限制**：
```bash
# 在 Cursor 设置中增加 max memory
```

---

## ❓ 常见问题

### Q1: 两个窗口同时修改同一文件会怎样？

**答**：Cursor 会提示文件被外部修改，您可以选择：
- 重新加载文件（丢失当前更改）
- 保留当前更改（覆盖外部更改）
- 比较差异后手动合并

**建议**：避免同时编辑同一文件

### Q2: Git 操作会影响其他窗口吗？

**答**：会的。所有窗口共享同一个 Git 仓库。
- 一个窗口切换分支，其他窗口也会提示
- 一个窗口提交，其他窗口会检测到变化
- **建议**：每个窗口使用不同分支

### Q3: 如何在多个窗口间共享代码片段？

**答**：
1. 使用 Git 提交和拉取
2. 复制粘贴
3. 使用代码片段工具
4. 使用共享的 workspace snippets

### Q4: 多窗口会消耗多少资源？

**答**：
- 每个窗口: ~200-500MB 内存
- 3 个窗口约: ~1-2GB
- 建议配置: 16GB+ RAM

### Q5: 可以在不同电脑上打开同一项目吗？

**答**：可以！通过 Git 同步：
```bash
# 电脑 1
git push origin feature/my-work

# 电脑 2
git pull origin feature/my-work
```

---

## 🎯 总结

### ✅ 可以做的

- 同时打开多个 Cursor 窗口
- 在不同窗口编辑不同文件
- 使用不同分支避免冲突
- 运行多个开发服务器（不同端口）

### ❌ 应该避免的

- 同时编辑同一个文件
- 在多个窗口频繁切换分支
- 忽略 Git 状态提示
- 开太多窗口导致系统卡顿

### 🎨 推荐工作流

```
1. 规划任务 → 创建分支
2. 每个窗口 → 不同分支/模块
3. 独立开发 → 频繁提交
4. 定期合并 → 主分支
5. 代码审查 → 独立窗口
```

---

## 📚 相关资源

- Git 分支管理：https://git-scm.com/book/zh/v2
- Cursor 文档：https://cursor.sh/docs
- VS Code Multi-root Workspaces：https://code.visualstudio.com/docs/editor/multi-root-workspaces

---

**开始使用多窗口开发，提升您的生产力！** 🚀

